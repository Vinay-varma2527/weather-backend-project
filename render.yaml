services:
  - type: web
    name: weather-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: WEATHER_API_KEY
        sync: false